import { Customcolor } from './customcolor';

describe('Customcolor', () => {
  it('should create an instance', () => {
    const directive = new Customcolor();
    expect(directive).toBeTruthy();
  });
});
